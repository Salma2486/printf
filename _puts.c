#include "main.h"
/**
 * put_s - adwwadawda
 * @string:dawdadaw
 * Return: r_val
 */
int put_s(char *string)
{
	int i = 0;

	if (string)
	{
		while (string[idx] != '\0')
		{
			putchr(string[i]);
			i++;
		}
	}
	return (0);
	else
		return (-1);
}
