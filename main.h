#ifndef _MAIN_H
#define _MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int _printf(const char *format, ...);
int putchr(char z);
int _puts(char *string);
void get_int(int num);
#endif
